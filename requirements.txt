# Recomendado usar Python 3.10–3.11
# Notas:
# - mediapipe instala OpenCV (contrib) como dependencia; evitamos conflictos con opencv-python
# - numpy<2.0 para evitar incompatibilidades en paquetes binarios precompilados

numpy>=1.24,<2.0
opencv-contrib-python>=4.8,<4.10
mediapipe>=0.10.14,<0.11